export interface IRoles {
  rolId: string;
  nombreRol: string;
  accesoTotal: number;
}

export interface IAgregarRol {
  nombreRol: string;
  accesoTotal: number;
}

export interface IEditarRol {
  rolId: string;
  nombreRol: string;
  accesoTotal: number;
}
